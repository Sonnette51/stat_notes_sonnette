---
title: "P4-P7 类神经网络训练不起来怎么办：总导读"
tags:
  - 李宏毅机器学习
  - optimization
  - neural-network
  - training-diagnosis
  - optimizer
  - loss-function
---
总体排查框架：

```mermaid
flowchart TD
    A["训练不起来 / training loss 不降"] --> B{"Gradient norm 是否接近 0?"}
    B -- "接近 0" --> C["P4: Critical point 分析"]
    C --> D{"Hessian eigenvalue"}
    D -- "全正" --> E["Local minimum"]
    D -- "有正有负" --> F["Saddle point"]
    F --> G["负 eigenvalue 对应下降方向"]
    B -- "不接近 0" --> H["P6: 可能是 learning rate / optimizer 问题"]
    H --> I["陡峭方向震荡 / 平坦方向走太慢"]
    I --> J["AdaGrad / RMSProp / Adam / Schedule"]
    A --> K["P5: Batch 改变 gradient 噪声与训练路径"]
    K --> L["Small batch: noisy but frequent updates"]
    K --> M["Momentum: 不只看当前 gradient"]
    M --> J
    A --> N["P7: Loss function 也会塑造 error surface"]
    N --> O["分类任务: one-hot / logits / softmax / cross entropy"]
```



## 从 Gradient Descent 的三个问题看四讲

Gradient descent 看似只有一行：

$$
\theta^{t+1} = \theta^t - \eta g^t
$$

但这行公式背后有三个问题：

| 问题 | 对应讲次 | 解释 |
|---|---|---|
| $g^t$ 是否还存在？ | P4 | 如果 $g^t \approx 0$，要看是不是 critical point，以及是哪种 critical point。 |
| $g^t$ 是否可靠？ | P5 | Mini-batch 让 gradient 带噪声；momentum 把过去方向纳入估计。 |
| $\eta g^t$ 的步伐是否合适？ | P6 | 同一个 $\eta$ 对所有参数可能不合适，需要 adaptive LR 和 schedule。 |
| $L$ 本身是否定义得好？ | P7 | 不同 loss function 会形成不同 error surface，影响 gradient 是否好用。 |
